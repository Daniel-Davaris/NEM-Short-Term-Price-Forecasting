## rules
- Start every response with "Daniel instructions acknowledged." + newline 
- Unless the user explicitly names a function/block to remove. Treat all existing code — including commented-out lines, alternative implementations, and exploratory cells — as intentional.
- When adding new behaviour, extend the existing code (new branch, new cell, additional column, optional parameter). Do not rewrite a function/cell from scratch when an additive change works.
- Keep cell boundaries, existing imports, variable names, and ordering. Add new imports/constants alongside existing ones rather than reorganizing.
- If a change seems to require deletion or a rewrite, stop and ask first.
- Leave spacing / whitespace as is, do not attempt to inline '=' over multiple lines or create more whitespace
- Always make changes directly to the python files, notebooks etc, never provide the code in the chat unless explicitly asked to do so
- When generating code, generate it with compact line spacing, don't expand a line of code onto multiple lines unless its most compact form is longer than the window
- When i ask for something or you are presented with a problem. Always go with a simpler code solution,the less code the better, and don't add anything that is not critical
- If I ask for simple changes, do not overcomplicate it 
